{
  "name": "runator",
  "version": "1.0.0",
  "description": "A web app that compiles C code to check for compilation errors.",
  "main": "server/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Flávio Coutinho",
  "license": "MIT",
  "dependencies": {
    "decompress": "^2.2.1",
    "fs-extra": "^0.18.0",
    "handlebars": "^3.0.1",
    "hapi": "^8.4.0",
    "socket.io": "^1.3.5"
  }
}
